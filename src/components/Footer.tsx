import React, { ReactElement } from "react";

const Footer = (): ReactElement => (
	<footer className="container absolute bottom-0 max-w-none text-center">
		<h6>I'm here to stay (Footer)</h6>
	</footer>
);

export default Footer;
